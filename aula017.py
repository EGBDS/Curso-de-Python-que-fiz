'''lanche = ['arroz', 'feijão']
lanche.append('cookie') # append adiciona mais uma objeto em lanche, no final da lista
lanche.insert(0, 'cachorro quente') # insert adiciona uma objeto em qualquer locar de lanche, alterando a posicão dos demais
del lanche[3]
lanche.pop(3) # del e pop eliminão o objeto com a numeração
lanche.remove('cookie') # remove elimina pelo nome em si do objeto
valores = list(range(4,11))
valores.sort() # sort vai colocar os valores em ordem crescente
valores.sort(reverse=True) # ira mostrar os valores em ordem decrecente'''

