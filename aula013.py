'''for c in range(0,10):
    passo
pega'''
'''for c in range(0,3): # c = contagem, pode ser qualquer palavra ou letra
    if moeda.moeda: #moeda.moeda é qualquer objeto
        pega
    passo
    pula
passo
pega'''
s = 0
for c in range(0,4):
    n = int(input('Digite um número: '))
    s = s + n
print('A soma de todos os números são {}'.format(s))